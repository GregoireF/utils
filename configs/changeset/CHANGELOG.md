# @gregoiref/changeset-config

## 0.0.1

### Patch Changes

- 201bc8c: Initial release: `createConfig(overrides?)` factory that generates a `.changeset/config.json` object with opinionated defaults for TypeScript monorepos.
