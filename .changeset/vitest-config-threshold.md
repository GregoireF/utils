---
"@gregoiref/vitest-config": patch
---

Raise coverage thresholds from 90% to 100% across all metrics (statements, branches, functions, lines). Any package using this config now enforces full coverage in CI.
